{
  "name": "roomicheck-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/web"],
  "scripts": {
    "dev:web": "npm run dev --workspace=@roomicheck/web",
    "build:web": "npm run build --workspace=@roomicheck/web",
    "test:web": "npm run lint --workspace=@roomicheck/web"
  }
}
